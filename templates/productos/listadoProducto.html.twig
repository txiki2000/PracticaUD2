{% extends 'basepractica2.html.twig' %}

{% block titulo %}Listado - {{ parent() }}{% endblock %}
{% block cabecera %}
        <h1 class="display-8">Listado de productos</h1>
{% endblock %}
{% block contenido %}
<div class="container text-centered">
        <table class="table table-hover">
            <thead class="thead-dark">
                <tr>
                    <th>Código</th>
                    <th>Nombre</th>
                    <th>Stock</th>
                    <th>Precio</th>
                </tr>
            </thead>
            <tbody>
                {% for i in range(0, (tabla | length) - 1) %}
                <tr>
                    <td>{{ tabla[i].codigo }}</td>
                    <td>{{ tabla[i].nombre }}</td>
                    <td>{{ tabla[i].stock }}</td>
                    <td>{{ tabla[i].precio }}</td>
                </tr>
                    
                {% endfor %}
            </tbody>
        </table>

    </div>
{% endblock %}
